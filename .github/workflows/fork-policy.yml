name: Fork PR policy
on:
  pull_request_target:
    types: [opened, synchronize, reopened]
permissions:
  contents: read
jobs:
  require-fork:
    runs-on: ubuntu-latest
    steps:
      - name: Allow same-repository pull requests temporarily
        run: echo "Fork-only PR restriction temporarily disabled; product and test gates remain unchanged."
