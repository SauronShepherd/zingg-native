resources:
  jobs:
    zingg_native_serverless_numeric_differential_probe:
      name: zingg-native-serverless-numeric-differential-probe
      description: STRICT numeric similarity differential against pinned Zingg 0.7 classes.
      timeout_seconds: 300
      tasks:
        - task_key: numeric_differential
          spark_jar_task:
            main_class_name: ai.zingg.nativebridge.launch.DatabricksZinggMainEntry
            parameters:
              - --native-mode
              - STRICT
              - --native-numeric-differential-probe
          environment_key: serverless_env5
      environments:
        - environment_key: serverless_env5
          spec:
            environment_version: "5"
            java_dependencies:
              - ${var.serverless_launcher_jar}
              - ${var.native_core_jar}
              - ${var.zingg_patched_jar}
